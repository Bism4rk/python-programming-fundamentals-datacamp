# Create the normal variable: normal
normal = 'simple'

# Create the mistaken variable: error
error = 'trailing comma',

# Print the types of the variables
print(type(normal))
print(type(error))

'''
O código acima demonstra como vírgulas podem afetar a criação de variáveis em Python. A variável `normal` é uma string simples, enquanto `error` é uma tupla com um único elemento devido à vírgula no final. A função `type()` é usada para verificar o tipo de cada variável, mostrando que `normal` é do tipo `str` e `error` é do tipo `tuple`.
'''